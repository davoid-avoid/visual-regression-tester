let screenPackage = {
    "type": "curated",
    "cover": true,
    "list": [
        {
            "screen": "example-spec Google Homepage",
            "packageScreen": "google home page"
        }
    ]
}

//alternatively, instead of a curated listing of screens, you can just set type type to "all", and all screenshots will be taken and added to the package.

module.exports = {
    screenPackage
}
